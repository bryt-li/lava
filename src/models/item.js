import { routerRedux } from 'dva/router';
import { parse } from 'qs';


export default {

  namespace: 'item',

  state: null,

  subscriptions: {
    setup({ dispatch, history }) {
    },
  },

  effects: {
  },

  reducers: {
  },

};
